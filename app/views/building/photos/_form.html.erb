<%= form_for(@building_photo) do |f| %>
  <% if @building_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@building_photo.errors.count, "error") %> prohibited this building_photo from being saved:</h2>

      <ul>
      <% @building_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
