<%= form_for(@building_status) do |f| %>
  <% if @building_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@building_status.errors.count, "error") %> prohibited this building_status from being saved:</h2>

      <ul>
      <% @building_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
