<%= form_for(@building_ammenity) do |f| %>
  <% if @building_ammenity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@building_ammenity.errors.count, "error") %> prohibited this building_ammenity from being saved:</h2>

      <ul>
      <% @building_ammenity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
