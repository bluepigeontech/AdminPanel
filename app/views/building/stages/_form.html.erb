<%= form_for(@building_stage) do |f| %>
  <% if @building_stage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@building_stage.errors.count, "error") %> prohibited this building_stage from being saved:</h2>

      <ul>
      <% @building_stage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
