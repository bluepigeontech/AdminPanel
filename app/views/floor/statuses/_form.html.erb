<%= form_for(@floor_status) do |f| %>
  <% if @floor_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@floor_status.errors.count, "error") %> prohibited this floor_status from being saved:</h2>

      <ul>
      <% @floor_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
