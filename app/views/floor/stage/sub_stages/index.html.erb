<p id="notice"><%= notice %></p>

<h1>Listing Floor Stage Sub Stages</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @floor_stage_sub_stages.each do |floor_stage_sub_stage| %>
      <tr>
        <td><%= link_to 'Show', floor_stage_sub_stage %></td>
        <td><%= link_to 'Edit', edit_floor_stage_sub_stage_path(floor_stage_sub_stage) %></td>
        <td><%= link_to 'Destroy', floor_stage_sub_stage, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sub stage', new_floor_stage_sub_stage_path %>
