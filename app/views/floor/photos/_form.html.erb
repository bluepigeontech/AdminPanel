<%= form_for(@floor_photo) do |f| %>
  <% if @floor_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@floor_photo.errors.count, "error") %> prohibited this floor_photo from being saved:</h2>

      <ul>
      <% @floor_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
