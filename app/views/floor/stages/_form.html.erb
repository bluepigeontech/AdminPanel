<%= form_for(@floor_stage) do |f| %>
  <% if @floor_stage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@floor_stage.errors.count, "error") %> prohibited this floor_stage from being saved:</h2>

      <ul>
      <% @floor_stage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
