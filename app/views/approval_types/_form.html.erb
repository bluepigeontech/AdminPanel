<%= form_for(@approval_type) do |f| %>
  <% if @approval_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@approval_type.errors.count, "error") %> prohibited this approval_type from being saved:</h2>

      <ul>
      <% @approval_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
