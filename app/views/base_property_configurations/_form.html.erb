<%= form_for(property_configuration) do |property_configuration_form| %>
   <% if property_configuration.errors.any? %>
      <div id="error_explanation">
        <div><%= pluralize(property_configuration.errors.count, "error") %> prohibited this property_configuration from being saved:</div>

        <ul>
        <% property_configuration.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <%= label_tag(:name, "Name") %>
    <%= property_configuration_form.text_field :name, :placeholder => "1 RK" %>
    
      
    <% if access_projects_edit %>
     <button class="btn cyan waves-effect waves-light" type="submit" name="action">Submit</button>
    <% end %>
<% end %>