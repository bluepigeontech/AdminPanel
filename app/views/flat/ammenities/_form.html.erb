<%= form_for(@flat_ammenity) do |f| %>
  <% if @flat_ammenity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flat_ammenity.errors.count, "error") %> prohibited this flat_ammenity from being saved:</h2>

      <ul>
      <% @flat_ammenity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
