<%= form_for(@flat_stage) do |f| %>
  <% if @flat_stage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flat_stage.errors.count, "error") %> prohibited this flat_stage from being saved:</h2>

      <ul>
      <% @flat_stage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
