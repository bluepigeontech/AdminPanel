<%= form_for(@project_offer) do |f| %>
  <% if @project_offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_offer.errors.count, "error") %> prohibited this project_offer from being saved:</h2>

      <ul>
      <% @project_offer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
