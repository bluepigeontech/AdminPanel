<%= form_for(@project_project_type) do |f| %>
  <% if @project_project_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_project_type.errors.count, "error") %> prohibited this project_project_type from being saved:</h2>

      <ul>
      <% @project_project_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
