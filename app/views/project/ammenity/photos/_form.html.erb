<%= form_for(@project_ammenity_photo) do |f| %>
  <% if @project_ammenity_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_ammenity_photo.errors.count, "error") %> prohibited this project_ammenity_photo from being saved:</h2>

      <ul>
      <% @project_ammenity_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
