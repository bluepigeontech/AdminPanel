<p id="notice"><%= notice %></p>

<h1>Listing Project Ammenity Photos</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @project_ammenity_photos.each do |project_ammenity_photo| %>
      <tr>
        <td><%= link_to 'Show', project_ammenity_photo %></td>
        <td><%= link_to 'Edit', edit_project_ammenity_photo_path(project_ammenity_photo) %></td>
        <td><%= link_to 'Destroy', project_ammenity_photo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Photo', new_project_ammenity_photo_path %>
