<%= form_for(@project_ammenity) do |f| %>
  <% if @project_ammenity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_ammenity.errors.count, "error") %> prohibited this project_ammenity from being saved:</h2>

      <ul>
      <% @project_ammenity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
