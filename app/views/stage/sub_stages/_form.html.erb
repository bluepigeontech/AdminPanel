<%= form_for(@stage_sub_stage) do |f| %>
  <% if @stage_sub_stage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stage_sub_stage.errors.count, "error") %> prohibited this stage_sub_stage from being saved:</h2>

      <ul>
      <% @stage_sub_stage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
